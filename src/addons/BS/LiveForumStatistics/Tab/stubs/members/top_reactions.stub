<ul class="content {$tab.tab_class}">
    <xf:if is="$users is not empty">

        <xf:foreach loop="$users" value="$user">
            <xf:set var="$rightIcon">
				<span title="{{ phrase('reactions:') }} {$topReactions.{$user.user_id}.reaction_score}" data-xf-init="tooltip">
					<xf:fa icon="far fa-smile-plus" /> {$topReactions.{$user.user_id}.reaction_score}
				</span>
            </xf:set>

            <xf:macro template="lfs_tab_member_definition_macros"
                      name="user_item"
                      arg-user="{$user}"
                      arg-date="{$user.register_date}"
                      arg-rightIcon="{$rightIcon}" />
        </xf:foreach>

        <xf:else />

        <xf:macro template="lfs_macros" name="empty_block" />

    </xf:if>
</ul>